# Phonebook Backend (Full Stack Phonebook)

This repository contains the backend for the Phonebook full-stack exercise.
The backend is built with Node.js and Express and serves both the REST API and the production frontend.

## Run locally (development)
1. Clone repository
2. Install dependencies:
   ```bash
   npm install
