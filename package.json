{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rimraf dist && cd \"../../Part 2/phonebook\" && npm run build && cp -r dist \"../Part 3/phonebook-backend\"",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "deploy": "git add . && git commit -m deploy && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "rimraf": "^6.1.2"
  }
}
